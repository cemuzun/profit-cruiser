POSTGRES_PASSWORD=change-me-to-something-random
PUBLIC_HOSTNAME=your-vps-ip.sslip.io

# Residential proxy fallback pool (comma-separated, format: HOST:PORT:USERNAME:PASSWORD)
# The scraper tries the direct VPS IP first (free). If blocked (0 results),
# it automatically rotates through these proxies in round-robin order.
# Leave empty to disable proxy fallback.
PROXY_LIST=
